{% block content %}

{% extends "layout.twig" %}
  {% for u in units %}
    {{ u.id }} 
    {{ u.name }} 
    {{ u.mineral }}
    {{ u.image_url }}  
    <br>
  {% else %}
      No units have been found.
  {% endfor %}

{% endblock %}
