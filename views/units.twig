{% extends "layout.html" %}
{% block content %}
  {{ parent() }}
  {% for u in units %}
    {{ u.id }} 
    {{ u.name }} 
    {{ u.mineral }}
    {{ u.image_url }}  
    <br>
  {% else %}
      No units have been found.
  {% endfor %}

{% endblock %}
